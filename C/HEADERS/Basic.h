//USER DEFINE HEADER FILE
#include<stdio.h>
#include<conio.h>
#include<math.h>
#include<string.h>
#include<stdlib.h>
int sum(int p,int q){
	return (p+q);
}
int square(int p){
	return (p*p);
}
float circle_area(float r){
	return (3.12*r*r);
}
float triangle_area(float b,float h){
	return (0.5*b*h);
}